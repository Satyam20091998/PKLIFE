version: 2

models:
  - name: src_customer
    columns:
      - name: ID
        data_type: number
        description: "id of customer"

      - name: NAME
        data_type: varchar
        description: 'name of customer'
        
      - name: SYSTEMMODSTAMP
        data_type: date
        description: 'date'