version: 2

sources:
  - name: new_src
    database: PL_DB
    schema: SOURCE
    tables:
      - name: raw_account
        columns:
          - name: ID
            data_type: int
            description: "id of customer"
          - name: NAME
            data_type: text
            description: 'name of customer'
          - name: SYSTEMMODSTAMP
            data_type: date
            description: 'date'

 